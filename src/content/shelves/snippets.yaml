description: >-
  Code I have pasted more than once: integer money arithmetic in TypeScript,
  retry with jitter, and a double-entry ledger schema in SQL. Each one written
  up with the reason it looks the way it does.
order: 4
groups:
  # src/content/snippets/<name>.md — the code fence carries its own copy button,
  # so the page offers no whole-document copy.
  - kind: docs
    title: snippets/
    meta: open to read, copy the block
    collection: snippets
    copyDocument: false
