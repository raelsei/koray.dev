---
import { cn } from '../../lib/cn';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<span
	class={cn(
		'text-nano tracking-tag border-edge text-soft border px-[9px] py-[5px] uppercase',
		className,
	)}
>
	<slot />
</span>
