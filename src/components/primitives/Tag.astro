---
import { cn } from '../../lib/cn';

interface Props {
	tone?: 'accent' | 'muted';
	class?: string;
}

const { tone = 'muted', class: className } = Astro.props;
---

<span
	class={cn(
		'text-nano tracking-tag border-edge border px-[9px] py-[5px] uppercase',
		tone === 'accent' ? 'text-lime' : 'text-soft',
		className,
	)}
>
	<slot />
</span>
